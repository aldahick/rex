extend type Query {
  config: Config!
}

type Config {
  # whether unauthenticated users can register a user account or not
  createAnonymousUsers: Boolean!
  # per-user total media limit in bytes
  mediaDataLimit: Int!
}
